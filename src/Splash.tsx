import styled from "styled-components";

const Splash = () => {
  const SplashContainer = styled.div`
    width: 100%
    height: 100%;
    posiotion: absolute;
    background-color: #aed3f4;
  `;

  return <SplashContainer>스플래시</SplashContainer>;
};

export default Splash;
